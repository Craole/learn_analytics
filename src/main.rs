mod config;
mod server;
mod tools;

fn main() {
    // Greet the current user
    // tools::say::hello();

    // Add the bin directory to PATH
    // tools::set_bin::main();
    // tools::init::env();

    // server::output::status_info();
    server::output::data_info();
}
